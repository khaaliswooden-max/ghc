# GS1 Integrations

| Subdirectory   | Purpose |
|----------------|---------|
| `epcis-2.0/`   | EPCIS 2.0 event capture/query bindings via OpenEPCIS. |
| `gdst/`        | Global Dialogue on Seafood Traceability profile mapping. |
| `gtin/`        | GTIN resolution (GS1 Digital Link). |

Conformance suites for the GHC EPCIS extension profile (see
`spec/01-data-model.md` §1.3) live in `tools/conformance/`.
